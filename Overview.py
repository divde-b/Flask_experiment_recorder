"""
一、网站功能说明
核心功能
展示实验记录
首页以表格形式列出所有已保存的实验，包括：ID、实验名称、日期、攻击机IP、靶机IP、网关IP、是否成功、备注、创建时间。
数据按日期倒序排列，最新的实验显示在最前面。
添加新实验
通过“添加新实验”链接进入表单页面，可以填写实验名称、日期、IP地址、成功状态、备注等信息。
提交后数据存入 MySQL 数据库，并自动跳回首页显示新记录。
技术栈
前端：HTML + 简单CSS（展示数据、提供表单）
后端：Python + Flask（处理请求、连接数据库）
数据库：MySQL（存储实验数据）
这就是一个典型的 小型全栈应用，麻雀虽小五脏俱全。现在已经跑通了从用户输入到数据存储再到展示的完整流程。

二、后续用树莓派构建微型服务器的意义
现在的项目跑在本地电脑上（127.0.0.1），只能自己访问。把它部署到树莓派上，意味着：
变成真正的服务器：树莓派24小时开机，你的网站就可以被局域网内任何人访问（甚至通过内网穿透让外网访问）
实践Linux运维：你需要安装操作系统（如Raspbian）、配置网络、安装MySQL和Python环境、部署Flask应用、设置开机自启、配置防火墙等——这些是真正的服务器运维技能。
拓展项目能力：可以在树莓派上继续完善项目，比如添加HTTPS、使用Nginx反向代理、搭配域名等。

三、树莓派部署的大致步骤（你现在可以开始了解）
硬件准备
树莓派4B（建议4GB内存以上）、电源、SD卡（至少16GB）、读卡器。
可选：散热片、外壳。
安装系统
用 Raspberry Pi Imager 将 Raspbian OS（现在叫 Raspberry Pi OS）刷入SD卡。
开启SSH（在Imager中提前设置好WiFi和SSH），方便无头连接。
基础配置
通过SSH登录树莓派，更新系统：sudo apt update && sudo apt upgrade -y
安装必要的软件：sudo apt install python3-pip python3-venv nginx git
部署应用
将Flask项目代码上传到树莓派（可以用git clone或scp）。
创建虚拟环境，安装依赖（pip install flask pymysql）。
安装MySQL：sudo apt install mariadb-server，并导入你的数据库。
修改数据库连接配置（密码、主机等）。
配置服务
使用 systemd 将Flask应用设置为后台服务，开机自启。
用Nginx反向代理Flask应用（处理静态文件、负载均衡等）。
网络访问
在局域网内通过树莓派IP访问。
如果想外网访问，可以用 frp 或 ngrok 内网穿透，或者申请公网IP、配置DDNS。

四、建议
先把现有项目功能完善（比如加删除、搜索、统计），让它更像一个完整作品。
买树莓派之前，可以先在虚拟机里模拟部署：用VirtualBox装一个Linux虚拟机，在上面走一遍部署流程，这样即使没有硬件也能提前练习。
等树莓派到手后，带着“我要把亲手写的代码跑在真服务器上”的目标去做，每成功一步都是巨大的正反馈。
现在已经掌握了全栈基础，树莓派会是你从“学习者”变成“建造者”的跳板。

五、个人想法
（1.）后续我想要添加查看文章详情在操作列表栏里，用户点击后可以查看文章详情。
（2.）添加一个弹窗，这个弹窗再用户点击删除试验记录时弹出，提示请输入管理员密码，输入我设置的密码后才能被删除。
（3.）在网页顶部增加搜索框，输入关键词支持模糊匹配试验记录。
"""

